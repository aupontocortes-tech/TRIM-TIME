/** Nome do bucket no Supabase Storage (Trim Player). */
export const TRIMPLAY_AUDIO_BUCKET = "trimplay-audio"

/** Alinhado ao limite do bucket no Supabase / fallback servidor. */
export const TRIMPLAY_AUDIO_MAX_BYTES = 52_428_800
